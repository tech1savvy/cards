---
id: definition
noteId: 1777823471346
---

What is the `Character` class in Java?

---

The `Character` class is a wrapper class in Java that is used to perform operations on `char` data types. It provides a number of useful class (i.e., static) methods for manipulating characters.

---

The `Character` class is in the `java.lang` package.

---
id: is-digit
noteId: 1777823471372
---

How to check if a character is a digit in Java?

---

Use the `Character.isDigit()` method.

---

```java
char ch = '1';
if (Character.isDigit(ch)) {
    System.out.println(ch + " is a digit.");
}
```

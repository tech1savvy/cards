---
id: is-letter
noteId: 1777823471395
---

How to check if a character is a letter in Java?

---

Use the `Character.isLetter()` method.

---

```java
char ch = 'a';
if (Character.isLetter(ch)) {
    System.out.println(ch + " is a letter.");
}
```

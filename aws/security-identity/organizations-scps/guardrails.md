---
noteId: 1778456404653
---

# What are Guardrails in SCPs?

---

Guardrails are SCPs that prevent unsafe or expensive actions across accounts. For example, an SCP can allow only t2.micro EC2 instances and deny all other instance types.

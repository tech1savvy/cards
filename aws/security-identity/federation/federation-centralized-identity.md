---
noteId: 1778456403054
---

# How does federation provide centralized identity management?

---

Users are managed in a corporate directory instead of having separate IAM users. Identity lives in one place (e.g., Active Directory) and is reused across AWS accounts.

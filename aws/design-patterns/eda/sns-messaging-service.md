---
noteId: 1778456400188
---

# Why was SNS selected for messaging?

---

Amazon SNS was selected for its pub/sub model handling simple fan-out messaging. It was chosen over EventBridge because it is simpler, lower cost, and fits the basic fan-out requirement.

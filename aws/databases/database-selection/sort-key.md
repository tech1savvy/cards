---
noteId: 1778456398938
---

# What is a Sort Key in DynamoDB?

---

An optional attribute used with the partition key to form a composite key, enabling range queries (e.g., `PartitionKey + SortKey`).

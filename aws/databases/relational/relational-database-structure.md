---
noteId: 1778392169924
---

# How is a relational database structured?

---

Organizes data into **tables** consisting of **rows** (**records**) and **columns** (**attributes**). Tables are linked using common columns (e.g., an `AuthorID` appearing in both a `Books` table and an `Authors` table).

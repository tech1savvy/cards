### What is Amazon EC2 Instance Store?

---

Ephemeral block storage physically attached to the host. Data is lost on instance stop/termination. Used for temporary content — buffers, caches, scratch data, and applications that replicate data across instances (e.g. Hadoop).

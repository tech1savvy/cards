### What is Amazon EBS and how does it scale?

---

Amazon Elastic Block Store (EBS) is persistent, network-attached block storage. Volumes can scale up to 16 TB, and multiple volumes can attach to one instance (one-to-many). EBS Multi-Attach allows one volume on multiple instances (same AZ, specific types).

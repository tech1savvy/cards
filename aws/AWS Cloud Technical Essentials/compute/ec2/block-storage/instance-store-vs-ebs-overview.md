### What are the two block storage options for EC2?

---

Instance Store (internal/ephemeral) and Amazon EBS (external/persistent). Instance Store is physically attached to the host; EBS is network-attached.

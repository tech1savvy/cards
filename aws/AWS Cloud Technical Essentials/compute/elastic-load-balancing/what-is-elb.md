### What is Elastic Load Balancing (ELB)?

---

**Elastic Load Balancing (ELB)** automatically distributes incoming application traffic across multiple targets (EC2 instances, containers, IP addresses, Lambda functions), providing a single DNS entry point while shielding the system from individual instance failures.

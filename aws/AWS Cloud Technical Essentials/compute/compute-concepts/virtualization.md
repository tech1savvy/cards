### What is a Virtual Machine (VM)?

---

A **Virtual Machine (VM)** emulates a physical server, allowing you to install an HTTP server and run applications. Virtualization enables multiple VMs to run on a single physical host by sharing its hardware resources.

### What is Active-Passive High Availability?

---

Only one instance is active and serving traffic; the other is a passive standby. Used for stateful applications where session data is stored locally on the server.

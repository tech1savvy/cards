### AWS Systems Manager (SSM)

---

Management service for controlling infrastructure across:

- AWS instances
- on-premises servers

Works through the **SSM Agent** installed on managed nodes.

Managed node = EC2 instance or on-prem VM with SSM agent installed.

---
noteId: 1778456402422
---

# How does SSM Run Command work?

---

Runs commands on servers remotely without requiring SSH or RDP. Commands are defined as SSM documents (JSON/YAML scripts) and executed across managed instances.

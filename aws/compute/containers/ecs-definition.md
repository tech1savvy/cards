---
noteId: 1778456395105
---

# What is Amazon ECS?

---

Amazon Elastic Container Service (ECS) is a fully managed, AWS-native container orchestration service. It acts as the control plane — it schedules and orchestrates containers across a cluster. ECS can run containers on either EC2 instances (you manage) or Fargate (AWS manages).

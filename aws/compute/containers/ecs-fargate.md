---
noteId: 1778456395173
---

# What is ECS with Fargate?

---

ECS (scheduler) runs containers on Fargate compute. You do not manage servers at all — you just define CPU, memory, and the container image. AWS handles all infrastructure.

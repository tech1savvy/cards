---
noteId: 1778456395254
---

# What is AWS Fargate?

---

AWS Fargate is a serverless compute engine for containers (both ECS and EKS). It eliminates infrastructure management — no provisioning, patching, or cluster capacity management. AWS handles the underlying OS and environment. You define CPU and memory at the Task or Pod level. Built-in HA and scaling.

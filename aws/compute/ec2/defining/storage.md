---
noteId: 1778456396038
---

# How does Storage work in EC2?

---

EC2 instances can use Instance Store (temporary, ephemeral storage) or Amazon EBS (persistent block storage). The choice depends on whether data needs to survive instance stops and terminations.

---
noteId: 1778456396454
---

# What is Stop-Hibernate?

---

Stop-Hibernate saves RAM contents to the Amazon EBS root volume so the instance resumes where it left off. This is useful for applications with long startup times. Unlike a regular stop, RAM data is preserved.

---
noteId: 1778456398038
---

# What is a Lambda runtime?

---

A runtime is the language environment that executes your Lambda function code. Lambda provides built-in runtimes for Python, Node.js, Java, .NET, Go, and Ruby. Custom runtimes are also supported via the Runtime API.

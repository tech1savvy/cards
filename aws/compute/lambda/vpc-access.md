---
noteId: 1778456398473
---

# How does Lambda VPC access work?

---

By default, Lambda functions run in an AWS-managed network with no access to resources in your VPC. You can optionally configure a function to connect to a VPC, giving it access to private resources like RDS databases, ElastiCache clusters, or internal services.

---
noteId: 1778456397938
---

# What triggers can invoke a Lambda function?

---

Lambda can be invoked by HTTP requests (via API Gateway), file uploads (e.g., a file added to S3), database changes (e.g., updates to DynamoDB Streams), and in-app activity (events from mobile or web applications via Cognito or custom sources).

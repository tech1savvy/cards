---
noteId: 1778456397805
---

# What is a Lambda execution role?

---

An execution role is an IAM role that grants a Lambda function permissions to access other AWS services. You attach policies to this role to define what the function can do (e.g., read from DynamoDB, write to S3, publish to SNS).

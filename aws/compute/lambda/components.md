---
noteId: 1778456397538
---

# What are the key components of a Lambda function?

---

A Lambda function has three key components: a **trigger** that describes when the function runs (e.g., S3 upload, API call), the **code** that describes what the function does, and the **configuration** that describes how it runs (memory, timeout, IAM role, environment variables).

---
noteId: 1778456397738
---

# What is Amazon ECS?

---

Amazon Elastic Container Service (ECS) is an AWS-native container orchestration service. It manages the scheduling, deployment, and scaling of Docker containers across a cluster. ECS can run containers on either EC2 instances or Fargate.

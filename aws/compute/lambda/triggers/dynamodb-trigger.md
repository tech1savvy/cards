---
noteId: 1778456398272
---

# How does DynamoDB trigger Lambda?

---

DynamoDB Streams can invoke a Lambda function whenever data in a table changes (insert, update, delete). Used for real-time data replication, search index updates, and cross-region sync.

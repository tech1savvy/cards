---
noteId: 1778456397305
---

# How does ALB round-robin routing work?

---

Round-robin is the default routing algorithm for ALB. It sends requests sequentially to each target in the target group, distributing traffic evenly regardless of current load on each target.

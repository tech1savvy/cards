---
noteId: 1778392170798
---

# What is Vertical Scaling (Scaling Up)?

---

Increasing the capacity of an existing resource (e.g., changing from `t2.micro` to `t2.large`). Cons: hard limit, requires reboot, single point of failure remains.

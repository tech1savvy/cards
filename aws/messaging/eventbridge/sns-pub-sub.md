---
noteId: 1778456400254
---

# What are the key characteristics of Amazon SNS as a pub/sub service?

---

Amazon SNS uses a pub/sub topic model with push delivery to subscribers. It offers very high throughput, very low latency (~30 ms), basic filtering, and limited target types. Does not support SaaS integration or a schema registry.

###### What does the AWS Glue DataBrew profiler do?

---

Analyzes the entire dataset and suggests ways to shape the data correctly. Detects duplicate rows and identifies PII (credit cards, bank accounts, driver's licenses, names, addresses, dates of birth).

Profile output includes stats like % empty cells, duplicate count, and classified PII fields.

---
noteId: 1778320495165
---

###### What is the table location property in AWS Glue?

---

A property on Glue Data Catalog tables that points to where the actual data is stored (e.g. an S3 path). Tables are metadata only — the location property is how Glue knows where to find the data for queries.

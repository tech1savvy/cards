---
noteId: 1778320494164
---

###### What is data gravity?

---

Data is heavy and hard to move; applications are light and easy to move (especially with containers). Keep compute and query engines physically close to the data to minimize latency — single-digit milliseconds within the same AWS region.

---
noteId: 1778320497841
---

###### What is a federated query in Athena?

---

A query that joins data from tables across different services (e.g. S3 + DynamoDB + RDS) without copying data to S3 first. Uses Athena connectors to reach the source directly.

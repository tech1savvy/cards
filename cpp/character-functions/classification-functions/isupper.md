---
id: isupper
backward:
  - "[islower](islower.md)"
forward:
  - "[isspace](isspace.md)"
---

Which option checks if a character is an uppercase letter?

---

Use the `isupper()` function.

```cpp
#include <cctype>

int result = isupper(c); // Non-zero if c is uppercase
```

---
id: isdigit
backward:
  - "[isalpha](isalpha.md)"
forward:
  - "[islower](islower.md)"
---

Which option checks if a character is a decimal digit (0-9)?

---

Use the `isdigit()` function.

```cpp
#include <cctype>

int result = isdigit(c); // Non-zero if c is a digit
```

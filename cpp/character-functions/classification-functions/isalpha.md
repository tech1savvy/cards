---
id: isalpha
backward:
  - "[isalnum](isalnum.md)"
forward:
  - "[isdigit](isdigit.md)"
noteId: 1777823457696
---

Which option checks if a character is an alphabetic letter?

---

Use the `isalpha()` function.

```cpp
#include <cctype>

int result = isalpha(c); // Non-zero if c is a letter
```

---
id: islower
backward:
  - "[isdigit](isdigit.md)"
forward:
  - "[isupper](isupper.md)"
noteId: 1777823457796
---

Which option checks if a character is a lowercase letter?

---

Use the `islower()` function.

```cpp
#include <cctype>

int result = islower(c); // Non-zero if c is lowercase
```
